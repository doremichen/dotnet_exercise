<UserControl x:Class="BeautyBookingApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="300" Width="300">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- 帳號文字 -->
            <RowDefinition Height="Auto"/>
            <!-- 帳號輸入框 -->
            <RowDefinition Height="Auto"/>
            <!-- 密碼文字 -->
            <RowDefinition Height="Auto"/>
            <!-- 密碼輸入框 -->
            <RowDefinition Height="Auto"/>
            <!-- 登入按鈕 -->
        </Grid.RowDefinitions>

        <TextBlock Text="帳號" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox x:Name="txtUsername" Grid.Row="1" Height="30"/>

        <TextBlock Text="密碼" Grid.Row="2" Margin="0,10,0,5"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="3" Height="30"/>

        <Button Content="登入"
                Grid.Row="4"
                Margin="0,20,0,0"
                Height="35"
                Click="Login_Click"/>
    </Grid>
</UserControl>
