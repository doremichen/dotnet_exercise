<Window x:Class="UartClientApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:UartClientApp.ViewModels"
        mc:Ignorable="d"
        Title="UART Client" Height="650" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="UART 参数设定" Grid.Column="0" Margin="0,0,10,0" Padding="5">
                <StackPanel>
                    <TextBlock Text="Port Name:" Margin="0,5,0,2"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox Grid.Column="0" ItemsSource="{Binding AvailablePorts}" SelectedItem="{Binding SelectedPortName}"/>
                        <Button Grid.Column="1" Content="刷新" Margin="5,0,0,0" Padding="5,2" Command="{Binding RefreshPortsCommand}"/>
                    </Grid>

                    <TextBlock Text="Baud Rate:" Margin="0,8,0,2"/>
                    <ComboBox ItemsSource="{Binding BaudRateOptions}" SelectedItem="{Binding SelectedBaudRate}"/>

                    <TextBlock Text="Data Bits:" Margin="0,8,0,2"/>
                    <ComboBox ItemsSource="{Binding DataBitsOptions}" SelectedItem="{Binding SelectedDataBits}"/>

                    <TextBlock Text="Parity:" Margin="0,8,0,2"/>
                    <ComboBox ItemsSource="{Binding ParityOptions}" SelectedItem="{Binding SelectedParity}"/>

                    <TextBlock Text="Stop Bits:" Margin="0,8,0,2"/>
                    <ComboBox ItemsSource="{Binding StopBitsOptions}" SelectedItem="{Binding SelectedStopBits}"/>

                    <Button Content="{Binding ConnectButtonText}" Margin="0,15,0,0" Height="32" FontWeight="Bold" Command="{Binding ConnectCommand}"/>

                    <TextBlock Text="{Binding ConnectionStatus}" Foreground="{Binding ConnectionStatusColor}" HorizontalAlignment="Center" Margin="0,8,0,0" FontWeight="Bold"/>

                    <Grid Margin="0,15,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Content="清除接收" Margin="0,0,2,0" Padding="0,4" Command="{Binding ClearReceivedCommand}"/>
                        <Button Grid.Column="1" Content="清除 Log" Margin="2,0,2,0" Padding="0,4" Command="{Binding ClearLogCommand}"/>
                        <Button Grid.Column="2" Content="导出 Log" Margin="2,0,0,0" Padding="0,4" Command="{Binding ExportLogCommand}"/>
                    </Grid>
                </StackPanel>
            </GroupBox>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <GroupBox Header="接收数据 (Received Data)" Grid.Row="0">
                    <TextBox IsReadOnly="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" TextWrapping="Wrap" Background="#F8F8F8" Text="{Binding ReceivedData, Mode=OneWay}"/>
                </GroupBox>

                <GroupBox Header="发送数据 (Send Data)" Grid.Row="1" Margin="0,5,0,0">
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="Checksum 模式: " VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <ComboBox Width="180" VerticalAlignment="Center" ItemsSource="{Binding ChecksumTypeOptions}" SelectedItem="{Binding SelectedChecksumType}"/>
                        </StackPanel>

                        <TextBox Grid.Row="1" Grid.Column="0" Height="30" VerticalContentAlignment="Center" Text="{Binding SendDataText}"/>
                        <Button Grid.Row="1" Grid.Column="1" Content="发送" Width="70" Margin="5,0,0,0" IsEnabled="{Binding IsSendEnabled}" Command="{Binding SendCommand}"/>
                    </Grid>
                </GroupBox>
            </Grid>
        </Grid>

        <GroupBox Header="系统与通讯 Log (Log View)" Grid.Row="1">
            <TextBox IsReadOnly="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" 
                     FontFamily="Consolas" FontSize="12" Background="#1E1E1E" Foreground="#DCDCDC" TextWrapping="Wrap" Text="{Binding LogText, Mode=OneWay}"/>
        </GroupBox>
    </Grid>
</Window>
